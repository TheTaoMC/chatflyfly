# 💬 กลุ่มแชท — คู่มือวิธีใช้

แชทกลุ่มง่ายๆ บนเว็บ — เปิดลิงก์แล้วเข้าสู่ระบบด้วย Google ก็แชทได้เลย
ในแชทใช้**ชื่อเล่น**เท่านั้น (ไม่เปิดเผยชื่อจริง) — email/ชื่อจริง เก็บไว้หลังบ้านสำหรับผู้ดูแล
โค้ด 2 ไฟล์หลัก (`server.js` + `index.html`) ไม่พึ่ง dependency ใดๆ รันด้วย Node.js เท่านั้น

---

## 🚀 เริ่มใช้งาน

### 0. ตั้งค่า Google login (ทำครั้งเดียว — ดูหัวข้อ "🔐 Google login" ด้านล่าง)

ต้องมี `GOOGLE_CLIENT_ID` ก่อน ไม่งั้นผู้ใช้ login ไม่ได้

### 1. รัน server

```bash
npm run dev
```

แล้วเปิด `http://localhost:3000` ใน browser (มือถือ/คอมพิวเตอร์ก็ได้)

> **การตั้งค่าตัวย่นย่อ:** สร้างไฟล์ `.env.local` ในโฟลเดอร์โปรเจกต์ (server อ่านให้อัตโนมัติ ไม่ต้องติดตั้งอะไร):
> ```
> GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
> ADMIN_EMAIL=อีเมล Google ของผู้ดูแล
> ```
> หรือตั้ง env ตรงก็ได้ (Windows: `set GOOGLE_CLIENT_ID=...` ก่อนรัน) — env จริงชนะไฟล์เสมอ

### 2. เข้าแชท

1. เปิดหน้าเว็บ → หน้าต่าง **"เข้าร้านราเมง"** จะโผล่ขึ้นมา
2. กด **"เข้าสู่ระบบด้วย Google"** (บังคับ — ต้อง login ก่อนถึงจะแชทได้)
3. ชื่อเล่นจะสุ่มให้อัตโนมัติ (เช่น "ราเมง4821") — แก้ได้ตามใจ + เลือก**ตัวละคร (อีโมจิ)**
4. กด **"เข้าแชท"** → เข้าห้องได้เลย

> ชื่อเล่น**ผูกกับบัญชี Google** — คนอื่นแย่งชื่อคุณไม่ได้ แม้รู้ชื่อ (ชื่อว่างอัตโนมัติเมื่อเงียบเกิน 60 วิ)
> ชื่อเล่นไม่ต้องเป็นชื่อจริง — แชทนี้เน้นไร้ตัวตน (ชื่อจริง/email เก็บหลังบ้าน ผู้ดูแลเท่านั้นที่ดูได้)

### 3. ใช้กับเพื่อน

**ในบ้าน/ที่ทำงาน (WiFi เดียวกัน):** แชร์ `http://<IPเครื่องคุณ>:3000`
(หา IP ได้จากคำสั่ง `ipconfig` บน Windows / `ifconfig` บน Mac-Linux)

**นอกบ้าน (อินเทอร์เน็ต):** ใช้ tunnel ฟรี

```bash
# Cloudflare Tunnel (แนะนำ) — จะได้ลิงก์ https://xxx.trycloudflare.com
cloudflared tunnel --url http://localhost:3000
```

```bash
# หรือ ngrok
ngrok http 3000
```

> ⚠️ ข้อควรรู้:
> - ลิงก์ trycloudflare เป็น**แบบชั่วคราว** — รันใหม่ทุกครั้งได้ลิงก์ใหม่
> - ต้องเปิดเครื่อง + รัน server กับ tunnel ค้างไว้ ไม่งั้นเพื่อนเข้าไม่ได้
> - ใครเห็นหน้าเก่า/เจอปัญหา → **กด Ctrl+F5 (รีเฟรชแรงๆ) 1 ครั้ง** เสมอ

---

## ✨ ฟีเจอร์

### แชทข้อความ
- พิมพ์ข้อความแล้วกด **Enter** หรือปุ่ม **ส่ง** — ข้อความใหม่เด้งมาเองทุก ~1.5 วิ
- ข้อความตัวเองอยู่ฝั่งขวา (เขียว) คนอื่นอยู่ฝั่งซ้าย (ขาว)
- รองรับภาษาไทย + อีโมจิครบ

### 🛍️ ร้านตัวละคร (ฝั่งซ้าย)
- คนที่อยู่ในห้องจะโผล่เป็น**ตัวละคร pixel art** ยืนรวมกันในร้าน (วาดด้วย canvas ไม่พึ่งภาพภายนอก)
- สีตัวละครตามอีโมจิที่เลือกตอนตั้งชื่อ (🦊 = ส้ม, 🐸 = เขียว, 🦄 = ม่วง...)
- **ตัวเรา** มีกรอบทอง + ชื่อสีทองเด่น
- ออกจากห้อง (ปิดหน้า) → ตัวละครหายเองหลัง ~1 นาที
- บนมือถือ ร้านย้ายไปเป็นแถบแนวนอนด้านบน

### 📷 ส่งรูป / GIF
- กดปุ่ม **📷** → เลือกไฟล์จากเครื่อง → ส่งเลย (jpg / png / gif / webp, ไม่เกิน 5MB)
- คลิกรูปในแชท = เปิดดูเต็มจอ
- รูป**ถูกลบอัตโนมัติหลัง 5 นาที** ทั้งจากแชทและเซิร์ฟเวอร์

### GIF picker (Giphy)
- กดปุ่ม **GIF** → พิมพ์คำค้น (เช่น "laugh", "hello") → เลือก GIF → ส่งเลย
- GIF ถูกส่งผ่านระบบอัปโหลดเดียวกัน → ลบอัตโนมัติหลัง 5 นาทีเหมือนรูป

### 👤 หน้าโปรไฟล์
- **กดที่ตัวละครในร้านราเมง** หรือ **กดที่ชื่อในข้อความ** → ดูโปรไฟล์
- แสดง: อวตาร pixel, สถานะ 🟢 ออนไลน์/⚪ ออฟไลน์, เลเวล, จำนวนหัวใจ 💗, วันที่เข้าร่วม, ออนไลน์ล่าสุด
- **ไม่เปิดเผยตัวตน:** email/ชื่อจริง/sub ไม่มีในโปรไฟล์สาธารณะเด็ดขาด (มีเทสต์ยืนยัน) — ดูได้เฉพาะผู้ดูแลที่ `/admin`
- โปรไฟล์ตัวเองมีปุ่ม "เปลี่ยนชื่อ/อวตาร"

### 💗 กดหัวใจให้เพื่อน
- เปิดโปรไฟล์เพื่อน → กด **"💗 ให้หัวใจ"** → หัวใจสะสมไว้กับบัญชีเพื่อน (เก็บถาวรใน Supabase)
- ให้ได้ **1 ครั้ง/วัน** (ต่อคนที่ให้ — นับตามวันไทย, ใหม่เที่ยงคืน) — ให้แล้วปุ่มจะขึ้น "ให้แล้ววันนี้ 💗"
- ให้ตัวเองไม่ได้ / โดนแบนกดไม่ได้
- จำนวนหัวใจโชว์ในโปรไฟล์ + หน้า admin (คอลัมน์หัวใจ) — **ใช้ซื้อชิ้นส่วนอวตารได้ที่ร้านค้า** 🛒

### 🎨 แต่งตัว + ร้านค้าอวตาร
- อวตาร = **ชิ้นส่วน 5 ชั้น** pixel 16×16: 🍜 ชาม / 👕 ตัว / 🙂 หัว / 😊 หน้า / 🎩 หมวก — เปิดได้จากปุ่ม "🎨 แต่งตัว" ใน modal ตั้งชื่อ หรือปุ่มเปลี่ยนชื่อ/อวตารในโปรไฟล์ตัวเอง
- **ชิ้นส่วนฟรี** (มีให้ทุกคน): ชาม 3 แบบ, เสื้อ 3 สี, ผม 3 สี, ตา 3 สี, หมวกเชฟ/ฟาง — แต่งได้เลย
- **ร้านค้า** (แท็บ 🛒): ซื้อชิ้นส่วนพิเศษด้วย **หัวใจ** (ชามทองคำ 50💗, หมวกเชฟทอง 60💗, กิมหยง 10💗 ฯลฯ) — ซื้อแล้วเป็นของถาวรในคลัง ใส่เมื่อไหร่ก็ได้
- ชาม (สี/ลาย) โชว์เป็นชามราเมงใต้ตัวละครในร้าน — เปลี่ยนชิ้นส่วน = เปลี่ยนชามทันที
- อวตารเก่า (อีโมจิ) ยังแสดงเหมือนเดิม — พอแต่งตัวครั้งแรกจะกลายเป็นระบบชิ้นส่วนอัตโนมัติ

### 🛠 สตูดิโอวาดชุด (ที่หน้า /admin — วาดเองได้!)
- เปิด `http://localhost:3000/admin` → เข้าสู่ระบบ Google ด้วย `ADMIN_EMAIL` → เลื่อนลงมาส่วน **"🛠 สตูดิโอวาดชุด"**
- กด **💾 บันทึกชิ้นส่วน** → production เก็บรูป PNG ใน **Supabase Storage** และเก็บข้อมูลชิ้นส่วนในแถว `parts` ของ `app_state` โดยอัตโนมัติ — รีสตาร์ต/Deploy แล้วไม่หาย (ตั้งราคา = ขึ้นร้านค้า 🛒 ให้คนอื่นซื้อด้วยหัวใจได้)
- ส่วน **"🧩 ชิ้นส่วนวาดเอง"** ด้านล่าง = ดูทั้งหมด + ปุ่มลบ
- ตอนพัฒนาในเครื่องโดยไม่ได้ตั้ง Supabase ระบบจะใช้ `avatars/` และ `parts.json` เป็น fallback เท่านั้น

### 🏆 คะแนน + เลเวล (นักชิมมือใหม่ → ปรมาจารย์ราเมง)
- ทุกกิจกรรมได้คะแนน: ส่งข้อความ = **+2**, ส่งรูป/GIF/เสียง = **+3**
- เลเวลตามคะแนน (โค้งยากขึ้นเรื่อยๆ — คุยทั้งวันทุกวันที่โควต้า 100/วัน ต้องใช้ **~3 เดือน** ถึงขั้นสูงสุด): 0 = 🍙 นักชิมมือใหม่ · 300 = 🍜 นักกินประจำ · 900 = 🥢 นักชิมตัวจริง · 2100 = 🍥 เชฟฝึกหัด · 4500 = 🏆 เซียนราเมง · 9000 = 👑 ปรมาจารย์ราเมง
- โชว์ไอคอน+ชื่อเลเวล + **แถบความคืบหน้า** ใต้ตัวละครในร้านราเมง (poll ทุก 2 วิ)
- เลเวลอัพ → แจ้งเตือน "⬆️ เลเวลอัพ!" ให้เจ้าของบัญชีนั้น
- คะแนนเก็บถาวรใน Supabase (ข้าม restart/deploy)
- **กันฟาร์มคะแนน:** ส่งถี่เกินไป (ภายใน 5 วิของแต้มล่าสุด) → ข้อความขึ้นปกติ แต่**ไม่ได้คะแนน**
  + แต่ละคนได้**สูงสุด 100 แต้ม/วัน** (ถึงขั้นสูงสุด 800 ต้องใช้ ~8 วัน — ทุ่มทั้งคืนไม่ได้) — ครบโควต้าแล้วร้านจะขึ้น "วันนี้ได้ครบแต้มแล้ว 🛑"

### 🎤 ส่งเสียง (ข้อความเสียง)
- กดปุ่ม **🎤** → เริ่มอัด (ไมโครโฟน) → กด **🎤 อีกครั้ง** = หยุดแล้วส่งทันที
- อัดได้**สูงสุด 10 วิ** (ครบ 10 วิ ส่งให้อัตโนมัติ) — ระหว่างอัดปุ่มจะแดง + นับเวลา
- ข้อความเสียงเล่นได้ทันทีในแชท — **ลบอัตโนมัติหลัง 5 นาที** เหมือนรูป
- ไม่ให้สิทธิ์ไมโครโฟน → แจ้งเตือน ไม่พัง

### 👤 เปลี่ยนชื่อ
- กดปุ่มชื่อมุมขวาบน (เช่น "👤 ตั้งชื่อ") → modal ขึ้นพร้อมชื่อเดิมให้แก้
- ไม่อยากเปลี่ยน → ปิดได้ด้วยปุ่ม **✕** / คลิกพื้นหลัง / กด **ESC**
- (รอบแรกที่ยังไม่มีชื่อ จะปิด modal ไม่ได้ — ต้องตั้งชื่อก่อนถึงจะเข้าแชท)
- ชื่อที่เพิ่งเปลี่ยนทิ้งจะถูก**กันไว้ 24 ชม.** — คนอื่นแย่งชื่อนั้นไม่ได้ (กันแอบอ้างตัว) แต่เจ้าของเดิมขอคืนได้เสมอ
  พ้น 24 ชม. แล้วชื่อถึงจะว่างให้คนอื่นจอง

### 🔐 Google login (บังคับ)
- ทุกคนต้องเข้าสู่ระบบด้วย Google ก่อนจองชื่อ/ส่งข้อความ/ส่งรูป
- ในแชทเห็นแค่**ชื่อเล่น** — email/ชื่อจริง ไม่โผล่ให้ใครเห็น (ตรวจสอบด้วยเทสต์)
- login แล้วจำ session ใน browser (F5 ไม่ต้อง login ซ้ำ) — ถ้า server restart ต้อง login ใหม่ครั้งเดียว
- กด "ออกจากระบบ" ใน modal เพื่อล้าง session

### 🌈 สนับสนุนเว็บด้วย Stripe + ชื่อรุ้ง
- ผู้ใช้ต้อง login Google แล้วกด **"🌈 สนับสนุน"** — server จะล็อกอีเมล Google และแนบรหัสอ้างอิงสุ่มให้ Payment Link อัตโนมัติ
- เมื่อจ่ายอย่างน้อย ฿20 สำเร็จ Stripe webhook จะตั้ง `supporter: true` ให้บัญชีนั้น แล้วชื่อจะแสดงเป็นสีรุ้งถาวร
- รายการชำระเก่าที่รหัสอ้างอิงไม่ตรงยังกู้สิทธิ์ได้จากอีเมล Stripe ซึ่งตรงกับอีเมล Google (ต้องส่ง event ซ้ำหลังผู้ใช้ login)
- ระบบไม่ให้สิทธิ์จากหน้า redirect และไม่เชื่อข้อมูลจาก browser: ต้องเป็น webhook ที่ลายเซ็นถูกต้อง, สถานะ `paid`, สกุล `THB` และยอดถึงขั้นต่ำเท่านั้น
- ตั้ง webhook ใน Stripe ให้ส่ง event `checkout.session.completed` และ `checkout.session.async_payment_succeeded` มาที่ `https://<โดเมนเว็บ>/stripe-webhook`
- นำ **Signing secret** (`whsec_...`) ไปตั้งเป็น env `STRIPE_WEBHOOK_SECRET` ห้ามใส่ secret ลง Git หรือหน้าเว็บ
- ค่า Payment Link เริ่มต้นคือ `https://buy.stripe.com/8x2bJ3a3P7IOef2fsHbsc00`; เปลี่ยนได้ด้วย env `SUPPORT_PAYMENT_LINK`

### 🕵️ หลังบ้านผู้ดูแล (email/ชื่อจริง)
- ทุกครั้งที่ login ระบบบันทึก `sub` + email + ชื่อจริงลง Supabase (หลังบ้าน)
- ผู้ใช้ทั่วไปเห็นแค่ชื่อเล่น — เฉพาะบัญชี Google ที่ตรงกับ `ADMIN_EMAIL` ถึงดูได้ที่ `http://localhost:3000/admin`
  (เข้าสู่ระบบ Google แล้วระบบโหลดข้อมูล) — มีการ์ดสรุป (คนออนไลน์/เคย login/คนแบน/ข้อความ) + ช่องค้นหา
  (ชื่อเล่น/อีเมล/ชื่อจริง/ชื่อเก่า) + ข้อความล่าสุด 5 อัน + ตารางคนออนไลน์/เคย login/Blocklist พร้อมปุ่มแบน-ปลดแบน
- ตารางเคย login มีคอลัมน์ **เปลี่ยนชื่อ** — ดูชื่อเล่นเก่า (10 อันล่าสุด, เอาเมาส์ชี้ดูเวลา) ไว้ยืนยันตัวตนก่อนแบน
- เก็บถาวรข้าม restart/deploy ในแถว `identities` ของตาราง `app_state`

### 🚫 Ban / Blocklist (sub ของ Google เป็นกุญแจ)
- หน้า `/admin` → ใส่เหตุผล (ไม่บังคับ) → กด **แบน** ในตารางคนออนไลน์/คนที่เคย login
- โดนแบนแล้ว: เตะ session ทิ้งทันที (ชื่อในร้านหาย) + login ใหม่ไม่ได้ + ส่งข้อความ/รูปไม่ได้ (403)
- ปลดแบนได้ที่ตาราง **Blocklist** → กด **ปลดแบน**
- เก็บในแถว `banned` ของตาราง `app_state` — อยู่ถาวรข้าม restart/deploy

---

## ⚙️ การตั้งค่า (ในโค้ด)

| ตัวแปร | ค่าเริ่มต้น | หมายเหตุ |
|---|---|---|
| `PORT` (server.js) | `3000` | port ของ server |
| `MAX_MESSAGES` | `200` | เก็บข้อความล่าสุดกี่ข้อความใน memory |
| `MAX_TEXT` | `2000` | ความยาวข้อความสูงสุด (ตัวอักษร) |
| `MAX_IMG_BYTES` | `5MB` | ขนาดไฟล์รูปสูงสุด (เสียง 10 วิ ≈ ไม่เกินนี้) |
| `MAX_VOICE_MS` (index.html) | `10 วิ` | ความยาวเสียงสูงสุด (ครบแล้วส่งอัตโนมัติ) |
| `IMAGE_TTL_MS` | `5 นาที` | รูปอยู่ได้นานเท่าไร ก่อนลบอัตโนมัติ (env: `IMG_TTL_MS`) |
| `ACTIVE_TTL_MS` | `60 วิ` | ชื่อว่างเมื่อเจ้าของเงียบเกินเท่าไร (env: `NAME_TTL_MS`) |
| `NAME_RETIRE_MS` | `24 ชม.` | ชื่อที่เพิ่งถูกเปลี่ยนทิ้ง — คนอื่นแย่งไม่ได้จนกว่าจะพ้นเวลานี้ (เจ้าของเดิมขอคืนได้เสมอ) |
| `GOOGLE_CLIENT_ID` (env) | — | OAuth Client ID จาก console.cloud.google.com (จำเป็น — ผู้ใช้ login ไม่ได้ถ้าไม่มี) |
| `ADMIN_EMAIL` (env) | — | อีเมล Google ของผู้ดูแล `/admin` — ไม่ตั้ง = ปิดไม่ให้เข้าถึง |
| `GOOGLE_JWKS_URL` (env) | Google cert | ใช้ตอนทดสอบ local เท่านั้น (ไม่ต้องแตะ) |
| `IDENTITIES_FILE` (env) | `identities.json` | ไฟล์เก็บ email/ชื่อจริงหลังบ้าน (ใช้ตอนทดสอบได้) |
| `BANNED_FILE` (env) | `banned.json` | ไฟล์ blocklist ผู้ใช้ (sub + เหตุผล) |
| `SUPABASE_URL` (env) | — | Project URL ของ Supabase (production ต้องตั้ง) |
| `SUPABASE_SECRET_KEY` (env) | — | Secret API key `sb_secret_...` ของ Supabase (เก็บเฉพาะฝั่ง server) |
| `SUPABASE_STATE_TABLE` (env) | `app_state` | ชื่อตารางเก็บสถานะ ไม่ต้องตั้งถ้าใช้ชื่อตามคู่มือ |
| `SUPABASE_AVATARS_BUCKET` (env) | `avatars` | ชื่อ bucket เก็บ PNG อวตาร; ระบบสร้าง public bucket นี้ให้ครั้งแรกที่อัปโหลด |
| `SUPPORT_PAYMENT_LINK` (env) | ลิงก์ ChatFlyFly | Stripe Payment Link สำหรับปุ่มสนับสนุน |
| `STRIPE_WEBHOOK_SECRET` (env) | — | Signing secret ของ `/stripe-webhook` (จำเป็นก่อนเปิดรับเงินจริง) |
| `STRIPE_PAYMENT_LINK_ID` (env) | — | `plink_...` สำหรับจำกัด webhook ให้รับเฉพาะ Payment Link นี้ (แนะนำ) |
| `SUPPORT_MIN_AMOUNT` (env) | `2000` | ยอดขั้นต่ำหน่วยสตางค์ (`2000` = ฿20) ที่ปลดล็อกชื่อรุ้ง |
| `POINT_COOLDOWN_MS` (env) | `5000` | ได้คะแนน 1 ครั้งต่อช่วงเวลานี้ (กันสแปม) |
| `DAILY_POINT_CAP` (env) | `100` | โควต้าคะแนนสูงสุดต่อวันต่อคน |
| `GIPHY_KEY` (index.html) | — | API key Giphy ฟรี จาก developers.giphy.com (เลือก **API** ไม่ใช่ SDK) |

---

## 🧩 โครงสร้างโปรเจกต์

```
├── server.js        # HTTP server — แชท, Google login, จองชื่อ, อัปโหลดรูป, /users, /admin, polling
├── index.html       # UI ทั้งหมด — แชท, ร้านราเมง, GIF picker, modal login+ตั้งชื่อ
├── server.test.js   # เทสต์ (node:test) — verify Google JWT + โฟลว์บังคับ login
├── uploads/         # (สร้างอัตโนมัติ) เก็บรูปที่ส่ง — ล้างทุกครั้งที่ restart
├── identities.json  # local dev fallback เมื่อไม่ได้ตั้ง Supabase (ไม่ขึ้น Git)
├── banned.json      # local dev fallback เมื่อไม่ได้ตั้ง Supabase (ไม่ขึ้น Git)
├── parts.json       # fallback สำหรับ local dev เมื่อไม่ได้ตั้ง Supabase
├── Dockerfile       # (deploy) image Node — รัน node server.js
├── package.json     # (deploy) ข้อมูลโปรเจกต์ขั้นต่ำ
└── วิธีใช้.md        # ไฟล์นี้
```

## 🔐 ตั้งค่า Google login (ครั้งเดียว)

1. เข้า [console.cloud.google.com](https://console.cloud.google.com) → สร้างโปรเจกต์
2. **APIs & Services → Credentials → Create Credentials → OAuth client ID** → เลือก **Web application**
3. เพิ่ม **Authorized JavaScript origins**:
   - `http://localhost:3000` (ทดสอบ local)
   - URL จริงของคุณ เช่น `https://ชื่อบริการ.onrender.com`
4. ก๊อป Client ID (ยาวๆ ลงท้าย `.apps.googleusercontent.com`) → ตั้งเป็น env `GOOGLE_CLIENT_ID` ตอนรัน
5. ตั้ง `ADMIN_EMAIL` เป็นอีเมล Google ของผู้ดูแล → เปิด `http://localhost:3000/admin` แล้วเข้าสู่ระบบบัญชีนั้น
   (ใส่ทั้งสองค่าใน `.env.local` ได้เลย — server โหลดให้อัตโนมัติ)

> 🔒 ข้อมูลส่วนตัว: ห้ามนำ `SUPABASE_SECRET_KEY` หรือข้อมูลในแถว `identities` ไปใส่หน้าเว็บ/ขึ้น GitHub
> `GOOGLE_CLIENT_ID` และ `ADMIN_EMAIL` ไม่ใช่รหัสผ่าน แต่ `SUPABASE_SECRET_KEY` เป็นความลับและห้ามใส่ในหน้าเว็บหรือ GitHub

## 🚀 Deploy ขึ้น Render

1. ใน Render สร้าง **Web Service** แล้วเชื่อม GitHub repository นี้
2. เลือก Runtime เป็น **Docker** เพื่อใช้ `Dockerfile` ที่มีอยู่
3. เพิ่มค่าที่หน้า **Environment** อย่างน้อย `GOOGLE_CLIENT_ID`, `ADMIN_EMAIL`, `STRIPE_WEBHOOK_SECRET`, `SUPABASE_URL` และ `SUPABASE_SECRET_KEY`
4. Deploy แล้วใช้ URL `https://ชื่อบริการ.onrender.com`

> ข้อมูลบัญชีและสิทธิ์ชื่อรุ้งอยู่ใน Supabase จึงไม่ต้องซื้อ Persistent Disk ของ Render ส่วนไฟล์ JSON ใช้เฉพาะ local dev เมื่อไม่ได้ตั้งค่า Supabase


## 🔌 API endpoints

| Method | Path | หน้าที่ |
|---|---|---|
| GET | `/` | หน้าแชท |
| GET | `/messages?name=xxx` | ดึงข้อความทั้งหมด (poll ทุก 1.5 วิ) + ส่ง `name` = heartbeat |
| POST | `/claim` | จองชื่อก่อนเข้าแชท (`{name, avatar}`) — ชื่อซ้ำ → 409 |
| POST | `/heart` | กดหัวใจให้เพื่อน (`{name, session}`) — วันละ 1 ครั้ง → 409 ถ้าให้แล้ว |
| GET | `/shop` | รายการสินค้าร้านค้าอวตาร (ซื้อด้วยหัวใจ) |
| POST | `/shop/buy` | ซื้อชิ้นส่วนอวตาร (`{id, session}`) — ตัดหัวใจ + เพิ่มเข้าคลัง (owned) |
| GET | `/me` | สถานะ session นี้ (`?session=`) — nickname/avatar/owned/hearts |
| GET | `/parts` | ชิ้นส่วนที่วาดเอง (สตูดิโอ) — client ใช้เรนเดอร์อวตารทุกคน |
| POST | `/parts` | (ผู้ดูแล) บันทึกชิ้นส่วนวาดเอง — ต้องใช้ Google session ของ `ADMIN_EMAIL` |
| GET | `/users` | รายชื่อ + avatar + คะแนน/เลเวล/ความคืบหน้าของคนในห้อง (poll ทุก 2 วิ) |
| GET | `/profile?name=xxx` | โปรไฟล์สาธารณะ (เลเวล/คะแนน/สถิติ — ไม่มี email/ชื่อจริง) |
| POST | `/send` | ส่งข้อความ (`{name, text}`) |
| POST | `/upload` | อัปโหลดรูป (`{name, ext, img(base64), session}`) |
| GET | `/uploads/<file>` | เสิร์ฟรูป (กัน path traversal + nosniff) |
| POST | `/google-login` | ตรวจสอบ ID token จาก Google → คืน session (`{credential}`) |
| POST | `/support-link` | สร้าง Payment Link ที่ผูกกับบัญชี Google (`{session}`) |
| POST | `/stripe-webhook` | รับผลชำระเงินจาก Stripe และปลดล็อกชื่อรุ้ง |
| GET | `/admin/users` | (ผู้ดูแล) รายชื่อผู้ใช้ + identity — ต้องใช้ Google session ของ `ADMIN_EMAIL` |
| GET | `/admin` | หน้าเว็บผู้ดูแล (เข้าสู่ระบบ Google ด้วย `ADMIN_EMAIL` แล้วจัดการข้อมูล) |
| POST | `/admin/ban` | (ผู้ดูแล) แบนผู้ใช้ด้วย `{sub, reason?}` — เตะ session + login ไม่ได้ทันที |
| POST | `/admin/unban` | (ผู้ดูแล) ปลดแบน `{sub}` |

---

## 🛠️ แก้ปัญหาบ่อย

**ส่งข้อความแล้วไม่ขึ้น / ฟีเจอร์ใหม่ไม่เห็น**
→ เพื่อนที่เปิดหน้าไว้ต้อง **Ctrl+F5** 1 ครั้ง (หน้าเก่า cache อยู่)

**ชื่อโดนปลด ทั้งที่ยังอยู่**
→ browser ลดความถี่ timer ตอนอยู่ tab หลัง — กลับมาที่แท็บ poll จะทันที ชื่อกลับมาเอง

**รูปหายไปจากแชท**
→ ปกติครับ — ระบบลบรูปอัตโนมัติหลัง 5 นาที (ตั้งค่าได้ที่ `IMAGE_TTL_MS`)

**Restart server แล้วข้อความหาย**
→ ปกติครับ — ข้อความเก็บใน memory เท่านั้น (อัปเกรดเป็นฐานข้อมูลได้ ถ้าต้องการ)

**เปิดผ่าน tunnel แล้วหน้าโหลดช้า ~1-2 วิ**
→ เป็นรอบ polling 1.5 วิ + ความหน่วง tunnel — ปกติ (ถ้าอยากให้ไวขึ้น ต้องใช้ named tunnel / WebSocket)

---

## 📌 ข้อจำกัด (จงใจให้ง่าย)

- ข้อความเก็บใน **memory** — restart แล้วหาย (ไม่มีฐานข้อมูล)
- **ห้องเดียว** — ใครเปิด URL ก็แชทด้วยกันทั้งหมด ไม่มีแยกห้อง
- **ต้อง login Google ถึงจะแชทได้** — identity (email/ชื่อจริง) เก็บหลังบ้านสำหรับผู้ดูแลเท่านั้น
- ลิงก์ tunnel ฟรีเป็นแบบชั่วคราว — ต้องการลิงก์ถาวรให้ใช้ Cloudflare Named Tunnel หรือ deploy ขึ้น Render
